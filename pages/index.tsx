import React from 'react';
import { Checkbox } from '../components';

export default function Home() {
  return (
    <div>
      <main>
        use storybook instead
        <br /> <code>yarn storybook</code>
        <input type="checkbox" />
      </main>
    </div>
  );
}
